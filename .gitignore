node_modules/
*.log

# dist/ is deliberately NOT ignored — it is committed, and CI verifies it
# matches packages/. Consumers read what is committed and published, not what a
# build could have produced.

.env
.env.*
